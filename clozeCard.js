module.exports = ClozeCard

// CLOSE CARD CONSTRUCTOR

function CreateCloseCard(text, close) {
	this.text = fullText;
	this.partial = particalText
	this.close = close
	this.errorCheck = function(error) {
		if(error) {
			console.log("ERROR OCCURED: " + error);
		}
	}
}